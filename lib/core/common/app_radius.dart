import 'package:flutter/material.dart';

class AppRadius{
  AppRadius._();
  static const r8 = Radius.circular(8.0);
  static const r10 = Radius.circular(10.0);
  static const r12 = Radius.circular(12.0);
  static const r16 = Radius.circular(16.0);

  static const circle = BorderRadius.all(Radius.circular(1000));
}